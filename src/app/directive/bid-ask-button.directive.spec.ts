import { BidAskButtonDirective } from './bid-ask-button.directive';

describe('BidAskButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new BidAskButtonDirective();
    expect(directive).toBeTruthy();
  });
});
