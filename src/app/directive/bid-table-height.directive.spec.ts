import { BidTableHeightDirective } from './bid-table-height.directive';

describe('BidTableHeightDirective', () => {
  it('should create an instance', () => {
    const directive = new BidTableHeightDirective();
    expect(directive).toBeTruthy();
  });
});
