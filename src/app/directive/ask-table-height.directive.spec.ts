import { AskTableHeightDirective } from './ask-table-height.directive';

describe('AskTableHeightDirective', () => {
  it('should create an instance', () => {
    const directive = new AskTableHeightDirective();
    expect(directive).toBeTruthy();
  });
});
