import { NotiVisibilityDirective } from './noti-visibility.directive';

describe('NotiVisibilityDirective', () => {
  it('should create an instance', () => {
    const directive = new NotiVisibilityDirective();
    expect(directive).toBeTruthy();
  });
});
