import { RealTimeColorDirective } from './real-time-color.directive';

describe('RealTimeColorDirective', () => {
  it('should create an instance', () => {
    const directive = new RealTimeColorDirective();
    expect(directive).toBeTruthy();
  });
});
