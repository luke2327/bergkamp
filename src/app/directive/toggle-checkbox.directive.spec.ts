import { ToggleCheckboxDirective } from './toggle-checkbox.directive';

describe('ToggleCheckboxDirective', () => {
  it('should create an instance', () => {
    const directive = new ToggleCheckboxDirective();
    expect(directive).toBeTruthy();
  });
});
